type RoomMast {
  roomID: ID!
  name: String!
  description: String!
  subDescription: String!
  maxPeopleNum: Int!
  stockNum: Int!
  minOrderNum: Int!
  deletedAt: String
  inSale: Boolean
}

input RoomMastInput {
  roomID: ID!
  name: String!
  description: String!
  subDescription: String!
  maxPeopleNum: Int!
  stockNum: Int!
  minOrderNum: Int!
  deletedAt: String
  inSale: Boolean
}
